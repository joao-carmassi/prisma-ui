import { ShineBorder } from '@/components/ui/shine-border';
import {
  Card,
  CardHeader,
  CardTitle,
  CardDescription,
  CardContent,
  CardFooter,
} from '@/components/ui/card';
import { Button } from '@/components/ui/button';
import { JsonLd } from '@/components/seo/json-ld';
import { InstallSnippet } from '@/components/docs/install-snippet';

export const metadata = {
  title: 'Shine Border Card',
  description:
    'A card component wrapped with an animated shine border effect. Creates a sweeping light highlight on the border.',
  alternates: { canonical: '/docs/components/shine-border-card' },
};

<JsonLd
  data={{
    '@context': 'https://schema.org',
    '@type': 'TechArticle',
    headline: 'Shine Border Card — Prisma UI',
    description:
      'A card component with an animated sweeping shine border effect.',
    breadcrumb: {
      '@type': 'BreadcrumbList',
      itemListElement: [
        {
          '@type': 'ListItem',
          position: 1,
          name: 'Home',
          item: 'https://prisma-ui-xi.vercel.app',
        },
        {
          '@type': 'ListItem',
          position: 2,
          name: 'Docs',
          item: 'https://prisma-ui-xi.vercel.app/docs',
        },
        {
          '@type': 'ListItem',
          position: 3,
          name: 'Shine Border Card',
          item: 'https://prisma-ui-xi.vercel.app/docs/components/shine-border-card',
        },
      ],
    },
  }}
/>

# Shine Border

An animated shining radial-gradient border effect. Inspired by [Magic UI's Shine Border](https://magicui.design/docs/components/shine-border). Implemented with pure CSS — no external animation library required.

## Installation

<InstallSnippet registryUrl='https://prisma-ui-xi.vercel.app/components/shine-border.json' />

## Import

```tsx
import { ShineBorder } from '@/components/ui/shine-border';
import {
  Card,
  CardHeader,
  CardTitle,
  CardDescription,
  CardContent,
  CardFooter,
} from '@/components/ui/card';
```

---

## Preview

<ComponentPreview className='gap-6 items-start'>
  <Card className='relative w-[340px]'>
    <ShineBorder shineColor='#9c40ff' />
    <CardHeader>
      <CardTitle>Shine Border</CardTitle>
      <CardDescription>
        A radial gradient sweeps continuously around the border.
      </CardDescription>
    </CardHeader>
    <CardContent>
      <p className='text-sm text-muted-foreground'>
        {'Drop '}
        <code>{'<ShineBorder />'}</code>
        {' inside any Card with '}
        <code>{'relative'}</code>
        {'.'}
      </p>
    </CardContent>
    <CardFooter>
      <Button size='sm'>Action</Button>
    </CardFooter>
  </Card>
</ComponentPreview>

```tsx
<Card className='relative w-[340px]'>
  <ShineBorder shineColor='#9c40ff' />
  <CardHeader>
    <CardTitle>Shine Border</CardTitle>
    <CardDescription>
      A radial gradient sweeps continuously around the border.
    </CardDescription>
  </CardHeader>
  <CardContent>
    <p className='text-sm text-muted-foreground'>
      Drop &lt;ShineBorder /&gt; inside any Card with relative.
    </p>
  </CardContent>
  <CardFooter>
    <Button size='sm'>Action</Button>
  </CardFooter>
</Card>
```

---

## Multi-color Shine

Pass an array of colors to `shineColor` to create a rainbow-like shine effect.

<ComponentPreview className='gap-6 items-start'>
  <Card className='relative w-[340px]'>
    <ShineBorder
      shineColor={['#ffaa40', '#9c40ff', '#00d2ff']}
      borderWidth={2}
    />
    <CardHeader>
      <CardTitle>Multi-color Shine</CardTitle>
      <CardDescription>Array of colors with a thicker border.</CardDescription>
    </CardHeader>
    <CardContent>
      <p className='text-sm text-muted-foreground'>
        {'Multiple colors blend into the radial gradient sweep.'}
      </p>
    </CardContent>
  </Card>
</ComponentPreview>

```tsx
<Card className='relative'>
  <ShineBorder shineColor={['#ffaa40', '#9c40ff', '#00d2ff']} borderWidth={2} />
  ...
</Card>
```

---

## Slow Duration

<ComponentPreview className='gap-6 items-start'>
  <Card className='relative w-[340px]'>
    <ShineBorder shineColor='#00d2ff' duration={30} />
    <CardHeader>
      <CardTitle>Slow Shine</CardTitle>
      <CardDescription>Longer duration for a subtle effect.</CardDescription>
    </CardHeader>
    <CardContent>
      <p className='text-sm text-muted-foreground'>
        {'Increase '}
        <code>{'duration'}</code>
        {' for a slower, more ambient glow.'}
      </p>
    </CardContent>
  </Card>
</ComponentPreview>

```tsx
<Card className='relative'>
  <ShineBorder shineColor='#00d2ff' duration={30} />
  ...
</Card>
```

---

## Props

| Prop          | Type                 | Default   | Description                                |
| ------------- | -------------------- | --------- | ------------------------------------------ |
| `borderWidth` | `number`             | `1`       | Width of the shining border in pixels      |
| `duration`    | `number`             | `14`      | Duration of the shine animation in seconds |
| `shineColor`  | `string \| string[]` | `#000000` | Color(s) for the shine gradient            |
