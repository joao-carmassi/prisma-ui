import { JsonLd } from '@/components/seo/json-ld';

export const metadata = {
  title: 'Border Beam Card',
  description:
    'Border Beam Card has been replaced by AuraBeam with variant="beam". See the Aura Beam Card docs for the new API.',
  alternates: { canonical: '/docs/components/border-beam-card' },
};

<JsonLd
  data={{
    '@context': 'https://schema.org',
    '@type': 'TechArticle',
    headline: 'Border Beam Card — Prisma UI',
    description: 'Replaced by AuraBeam variant="beam".',
    breadcrumb: {
      '@type': 'BreadcrumbList',
      itemListElement: [
        { '@type': 'ListItem', position: 1, name: 'Home', item: 'https://prismaui.com' },
        { '@type': 'ListItem', position: 2, name: 'Docs', item: 'https://prismaui.com/docs' },
        { '@type': 'ListItem', position: 3, name: 'Border Beam Card', item: 'https://prismaui.com/docs/components/border-beam-card' },
      ],
    },
  }}
/>

# Border Beam

> **This component has been superseded by [`<AuraBeam variant="beam">`](/docs/components/aura-beam-card#beam).**
> The new component preserves the travelling-arc behavior while adding the high-quality glow and blur layers of the AuraBeam aesthetic.

## Migration

Replace the old `BorderBeam` usage:

```tsx
<Card className='relative overflow-hidden'>
  <BorderBeam />
  ...
</Card>
```

With the new `AuraBeam`:

```tsx
<AuraBeam variant='beam'>
  <Card className='rounded-[inherit] border bg-background'>
    ...
  </Card>
</AuraBeam>
```

For custom colors and duration:

```tsx
<AuraBeam variant='beam' color={['#ffaa40', '#9c40ff']} duration={4}>
  <Card className='rounded-[inherit] border bg-background'>
    ...
  </Card>
</AuraBeam>
```

See the full [Aura Beam Card documentation](/docs/components/aura-beam-card) for all available props.